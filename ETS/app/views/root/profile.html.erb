<%= 
ln( lt( content_tag(:h3, @header) ) ) +
ln( lt( content_tag(:p, @id+@employee.id) ) ) +
ln( lt( content_tag(:p, @name+@employee.name) ) ) +
ln( lt( content_tag(:p, @position+@employee.position) ) ) +
ln( lt( content_tag(:p, @workload+@employee.workload) ) ) +
ln( lt( content_tag(:p, @bank_of_hours+@employee.bank_hours) ) ) +
ln( lt( content_tag(:p, @last_reset+@employee.last_reset) ) ) +
( lt( content_tag(:p, @next_reset+@employee.next_reset) ) ) 
%>